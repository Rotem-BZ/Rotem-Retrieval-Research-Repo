# retrieval-components

The publishable component library for the retrieval research monorepo. It contains
reusable Haystack components and their component-level utilities, without experiment
orchestration.
