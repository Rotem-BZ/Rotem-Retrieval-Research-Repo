# @package _global_

defaults:
  - /paths: local
  - /dataset: ???
  - /pipeline/inference@pipeline: ???
  - /runtime: ???
  - _self_

stage:
  name: inference
  run_id: ${now:%Y%m%d-%H%M%S-%f}
  output_dir: ${paths.runs_dir}/${stage.name}/${stage.run_id}
  predictions_path: ${stage.output_dir}/predictions.json

selections:
  # Null means every dataset document is a candidate for every query.
  # Otherwise, set this to a folder name under paths.input_mappings_dir.
  input_mapping: null
