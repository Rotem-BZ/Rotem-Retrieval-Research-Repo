components:
  input:
    type: retrieval_components.interfaces.stage_io.InferenceInput
  output:
    type: retrieval_components.interfaces.stage_io.InferenceOutput
  fusion:
    type: retrieval_components.fusion.reciprocal_rank_fusion.ReciprocalRankFusion
    init_parameters:
      weights: ???
      top_k: 5
      rrf_k: 60

connections:
  - sender: fusion.documents
    receiver: output.documents
max_runs_per_component: 100
metadata:
  description: Abstract weighted RRF fusion topology. Add retrievers and connections with Hydra overrides.
