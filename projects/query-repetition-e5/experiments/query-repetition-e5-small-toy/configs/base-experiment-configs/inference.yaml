# @package _global_
defaults:
  - /stages/inference
  - override /dataset: toy
  - override /pipeline/inference@pipeline: dense_jsonl
  - override /selections/embedding_model@selections.embedding_model: e5/small_v2
  - override /runtime: cpu
  - _self_

paths:
  processed_data_dir: ${paths.project_root}/../../data/processed

selections:
  index_id: toy-e5-small-index
