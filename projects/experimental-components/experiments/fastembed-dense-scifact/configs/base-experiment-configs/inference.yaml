# @package _global_
defaults:
  - /stages/inference
  - override /dataset: beir_scifact
  - override /pipeline/inference@pipeline: dense_jsonl
  - override /selections/embedding_model@selections.embedding_model: fastembed/bge_small_en_v1_5
  - override /runtime: cpu
  - _self_

selections:
  index_id: fastembed-dense-scifact-st

pipeline:
  components:
    retriever:
      init_parameters:
        top_k: 100

